import { module, test } from 'qunit';
import { visit, currentURL, click } from '@ember/test-helpers';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | add numbers', function(hooks) {
    setupApplicationTest(hooks);

    test('should add provided numbers', async function (assert) {
    });

    test('should multiple provided numbers', async function (assert) {
    });

    test('should subtract provided numbers', async function (assert) {
    });

    test('should divide provided numbers', async function (assert) {
    });

    test('should update theme settings', async function (assert) {
    });
});
